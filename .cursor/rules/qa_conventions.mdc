---
alwaysApply: true
---
# QA Conventions - Правила разработки тестов

> **Базовые документы:** [vision.md](../../docs/vision.md) | [conventions.mdc](conventions.mdc)

## Принципы тестирования

### Test-Driven Development (TDD)

**Цикл Red-Green-Refactor:**
```
🔴 Red   → Пишем failing тест (определяет ожидаемое поведение)
🟢 Green → Минимальная реализация (тест проходит)
🔵 Refactor → Улучшаем код (тесты остаются зелеными)
```

**TDD workflow:**
1. Пишем тест для нового функционала (тест падает)
2. Реализуем минимум кода для прохождения теста
3. Рефакторим код (SOLID, DRY), тесты проходят
4. Повторяем для следующего функционала

### KISS (Keep It Simple, Stupid)

- **Тестируй только основное поведение** - не тестируй фреймворк
- **Один тест = один сценарий** - не комбинируй несколько проверок
- **Простые имена тестов** - `test_config_missing_token()`, не `test_1()`
- **Минимум моков** - мокируй только внешние зависимости (API, БД)

### DRY (Don't Repeat Yourself)

- **Используй фикстуры** - переиспользуй setup код
- **Вынеси общие моки** - в `conftest.py`
- **Константы для тестовых данных** - не хардкодь везде

### Что НЕ тестировать (избегай boilerplate)

❌ **НЕ тестируй:**
- Геттеры/сеттеры без логики (`@property` без вычислений)
- Тривиальные конструкторы (`__init__` без валидации)
- Стандартное поведение фреймворка (aiogram, openai)
- Private методы напрямую (тестируются через public API)
- Константы и enum'ы (если нет логики)

✅ **Тестируй:**
- Бизнес-логику (валидация, обработка данных)
- Граничные случаи (пустые значения, None, ошибки)
- Взаимодействие компонентов (интеграционные точки)
- Публичные API классов
- Обработку ошибок (exceptions, fallbacks)

## Структура теста (Arrange-Act-Assert)

### Формат AAA

```python
@pytest.mark.asyncio
async def test_process_message(mock_llm_client):
    """Тест: Обработка сообщения и сохранение в истории"""
    # Arrange: подготовка данных и зависимостей
    manager = ConversationManager(mock_llm_client, "System prompt", max_history=10)

    # Act: выполнение тестируемого действия
    response = await manager.process_message(123, "Hello")

    # Assert: проверка результата
    assert response == "Test response"
    mock_llm_client.get_response.assert_called_once()
    context = manager.storage.get_context(123)
    assert len(context.messages) == 1
```

### Правила AAA

- **Arrange** - все моки и данные создаются в начале
- **Act** - только одно действие (один вызов метода)
- **Assert** - все проверки в конце, используй `assert_called_once()`, `match=`

### Комментарии в тестах

- Комментарии обязательны для сложных моков
- Docstring описывает сценарий теста
- Используй пустые строки между AAA блоками

## Именование тестов

### Правила именования

**Файлы:** `test_<module_name>.py`
```
src/config.py      → tests/test_config.py
src/llm_client.py  → tests/test_llm_client.py
```

**Функции:** `test_<what>_<scenario>()`
```python
test_config_missing_token()           # Что + сценарий
test_get_response_api_error()         # Метод + ошибка
test_process_message_success()        # Метод + успех
test_handle_empty_message()           # Метод + граничный случай
```

**Docstring:**
```python
def test_config_invalid_max_tokens(valid_env, monkeypatch):
    """Тест: ValueError при невалидном MAX_TOKENS"""
```

### Паттерны именования

| Сценарий | Паттерн | Пример |
|----------|---------|--------|
| Успешное выполнение | `test_<method>_success()` | `test_get_response_success()` |
| Ошибка | `test_<method>_<error>()` | `test_get_response_api_error()` |
| Граничный случай | `test_<method>_<edge_case>()` | `test_handle_empty_message()` |
| Валидация | `test_<method>_invalid_<param>()` | `test_config_invalid_temperature()` |
| Отсутствие данных | `test_<method>_missing_<param>()` | `test_config_missing_token()` |

## Async тестирование

### pytest-asyncio

```python
import pytest
from unittest.mock import AsyncMock

@pytest.mark.asyncio
async def test_get_response_success(llm_client: LLMClient) -> None:
    """Тест успешного получения ответа от LLM"""
    # Arrange: подготовка mock
    mock_response = Mock(
        choices=[Mock(message=Mock(content="Test response"))],
        usage=Mock(total_tokens=50)
    )

    # Act: мокирование async метода
    with patch.object(
        llm_client.client.chat.completions, "create", new_callable=AsyncMock
    ) as mock_create:
        mock_create.return_value = mock_response
        result = await llm_client.get_response([{"role": "user", "content": "Hi"}])

        # Assert
        assert result == "Test response"
        mock_create.assert_called_once()
```

### Правила async тестов

- **`@pytest.mark.asyncio`** - обязательно для async функций
- **`AsyncMock`** - для мокирования async методов
- **Type hints** - `-> None` для всех тестовых функций
- **Фикстуры** - могут быть async (используй `async def` в фикстуре)

### Мокирование async API

```python
# Успешный вызов
mock_create.return_value = mock_response

# Симуляция ошибки
mock_create.side_effect = Exception("API Error: Rate limit exceeded")

# Проверка вызова
mock_create.assert_called_once_with(
    model="test-model",
    messages=messages,
    max_tokens=100,
    temperature=0.7,
)
```

## Фикстуры

### Базовые фикстуры (conftest.py)

```python
@pytest.fixture
def clean_env(monkeypatch):
    """Очистить переменные окружения для изоляции тестов"""
    monkeypatch.setattr("src.config.load_dotenv", lambda: None)
    monkeypatch.delenv("TELEGRAM_BOT_TOKEN", raising=False)
    # ... остальные переменные

@pytest.fixture
def valid_env(monkeypatch):
    """Установить валидные переменные окружения"""
    monkeypatch.setattr("src.config.load_dotenv", lambda: None)
    monkeypatch.setenv("TELEGRAM_BOT_TOKEN", "test_telegram_token")
    monkeypatch.setenv("OPENROUTER_API_KEY", "test_openrouter_key")
```

### Локальные фикстуры

```python
@pytest.fixture
def llm_client() -> LLMClient:
    """Фикстура с LLMClient для тестов"""
    return LLMClient(
        api_key="test-api-key",
        model="test-model",
        max_tokens=100,
        temperature=0.7,
    )

@pytest.fixture
def mock_llm_client():
    """Мок LLMClient для тестов ConversationManager"""
    client = AsyncMock()
    client.model = "test-model"
    client.get_response = AsyncMock(return_value="Test response")
    return client
```

### Правила фикстур

- **Общие фикстуры** - в `conftest.py` (например, `valid_env`)
- **Специфичные фикстуры** - в файле теста (например, `llm_client`)
- **Переиспользование** - фикстуры могут использовать другие фикстуры
- **Именование** - понятные имена, отражающие суть (`mock_llm_client`, не `client1`)

## Мокирование

### Стратегия мокирования

**Правило:** Мокируй внешние зависимости, не тестируй свой код

✅ **Мокируй:**
- Внешние API (OpenRouter, Telegram)
- Файловую систему (load_dotenv)
- Базы данных
- Системные вызовы (time, datetime)
- Зависимости между классами (при unit тестах)

❌ **Не мокируй:**
- Свой бизнес-код (тестируй реальную логику)
- Стандартные структуры данных (list, dict)
- Dataclass'ы и модели
- Simple getters/setters

### Типы моков

#### Mock для синхронного кода

```python
from unittest.mock import Mock, patch

mock_config = Mock()
mock_config.openrouter_key = "test-key"
mock_config.default_model = "test-model"
```

#### AsyncMock для async кода

```python
from unittest.mock import AsyncMock

mock_bot = Mock()
mock_bot.start_polling = AsyncMock(side_effect=KeyboardInterrupt())
mock_bot.stop = AsyncMock()
```

#### patch для подмены

```python
# Подмена метода объекта
with patch.object(
    llm_client.client.chat.completions, "create", new_callable=AsyncMock
) as mock_create:
    mock_create.return_value = mock_response
    result = await llm_client.get_response(messages)

# Подмена класса
with patch("src.main.Config") as mock_config_class:
    mock_config_class.return_value = mock_config
    await main()
```

#### side_effect для ошибок

```python
# Симуляция исключения
mock_create.side_effect = Exception("API Error: Rate limit exceeded")

# Проверка, что исключение пробросилось
with pytest.raises(Exception, match="API Error: Rate limit exceeded"):
    await llm_client.get_response(messages)
```

### Паттерны мокирования

#### Мок сложного API ответа

```python
# Arrange: структура OpenAI response
mock_message = Mock()
mock_message.content = "Test response from LLM"

mock_choice = Mock()
mock_choice.message = mock_message

mock_usage = Mock()
mock_usage.total_tokens = 50

mock_response = Mock()
mock_response.choices = [mock_choice]
mock_response.usage = mock_usage
```

#### Мок зависимостей в main()

```python
@pytest.mark.asyncio
async def test_main_success() -> None:
    """Тест успешного запуска приложения"""
    with (
        patch("src.main.Config") as mock_config_class,
        patch("src.main.LLMClient") as mock_llm_class,
        patch("src.main.ConversationManager") as mock_conv_class,
        patch("src.main.TelegramBot") as mock_bot_class,
    ):
        # Настройка моков
        mock_config = Mock()
        mock_config_class.return_value = mock_config

        mock_bot = Mock()
        mock_bot.start_polling = AsyncMock(side_effect=KeyboardInterrupt())
        mock_bot.stop = AsyncMock()
        mock_bot_class.return_value = mock_bot

        # Act
        await main()

        # Assert
        mock_config_class.assert_called_once()
        mock_bot.stop.assert_called_once()
```

## Coverage метрики

### Целевые показатели

| Тип модуля | Coverage цель | Примеры |
|------------|---------------|---------|
| Критичные модули | 100% | config.py, models.py |
| Бизнес-логика | >90% | llm_client.py, conversation_manager.py |
| Интеграционный код | >80% | telegram_bot.py, main.py |
| **Общий проект** | **>80%** | **Текущий: 95%** |

### Команды coverage

```bash
# Обычный запуск с coverage
make test

# Детальный HTML отчет
pytest --cov=src --cov-report=html
# Открыть htmlcov/index.html в браузере

# Coverage для конкретного модуля
pytest tests/test_llm_client.py --cov=src.llm_client --cov-report=term-missing
```

### Интерпретация coverage

- **100%** - все строки покрыты (критичный код)
- **>90%** - отличный уровень (бизнес-логика)
- **>80%** - хороший уровень (интеграции)
- **<80%** - требуется добавить тесты

**Пропускаем:**
- `if __name__ == "__main__"` - entry points
- Абстрактные методы (если нет реализации)
- Debug код (временный)

## Проверка качества тестов

### Обязательные проверки

```bash
# Перед коммитом - запусти все проверки
make quality

# Что выполняется:
# 1. make format   - автоформатирование
# 2. make lint     - ruff check (0 ошибок)
# 3. make typecheck - mypy (0 ошибок)
# 4. make test     - pytest + coverage
```

### Критерии качественных тестов

✅ **Хорошие тесты:**
- Быстрые (<1s на тест, кроме интеграционных)
- Изолированные (не зависят друг от друга)
- Понятные (имя теста = документация)
- Стабильные (не флакают)
- Поддерживаемые (легко изменить при рефакторинге)

❌ **Плохие тесты:**
- Медленные (>5s на unit тест)
- Зависимые (порядок выполнения важен)
- Непонятные (что тестирует `test_1()`?)
- Флакующие (иногда падают без причины)
- Хрупкие (сломаются при любом изменении)

### Type hints в тестах

```python
@pytest.fixture
def llm_client() -> LLMClient:
    """Фикстура должна иметь return type"""
    return LLMClient(api_key="test-key", model="test-model")

@pytest.mark.asyncio
async def test_get_response_success(llm_client: LLMClient) -> None:
    """Тесты должны иметь -> None"""
    result = await llm_client.get_response([{"role": "user", "content": "Hi"}])
    assert result == "Test response"
```

## Специфичные паттерны проекта

### Тестирование валидации Config

```python
def test_config_missing_telegram_token(clean_env, monkeypatch):
    """Тест: ValueError при отсутствии TELEGRAM_BOT_TOKEN"""
    monkeypatch.setenv("OPENROUTER_API_KEY", "test_key")

    with pytest.raises(ValueError, match="TELEGRAM_BOT_TOKEN"):
        Config()

def test_config_invalid_max_tokens(valid_env, monkeypatch):
    """Тест: ValueError при невалидном MAX_TOKENS"""
    monkeypatch.setenv("MAX_TOKENS", "not_a_number")

    with pytest.raises(ValueError, match="MAX_TOKENS должно быть целым числом"):
        Config()
```

### Тестирование обработчиков Telegram

```python
@pytest.mark.asyncio
async def test_handle_message_success(telegram_bot, mock_message):
    """Тест успешной обработки сообщения"""
    # Arrange
    mock_conversation_manager = AsyncMock()
    mock_conversation_manager.process_message = AsyncMock(
        return_value="LLM response"
    )
    telegram_bot.conversation_manager = mock_conversation_manager

    # Act
    await telegram_bot.handle_message(mock_message)

    # Assert
    mock_conversation_manager.process_message.assert_called_once_with(
        12345, "test message"
    )
    mock_message.answer.assert_called_once_with("LLM response")
```

### Тестирование обработки ошибок

```python
@pytest.mark.asyncio
async def test_handle_message_error(telegram_bot, mock_message):
    """Тест обработки ошибки при получении ответа LLM"""
    # Arrange
    mock_conversation_manager = AsyncMock()
    mock_conversation_manager.process_message = AsyncMock(
        side_effect=Exception("API Error")
    )
    telegram_bot.conversation_manager = mock_conversation_manager

    # Act
    await telegram_bot.handle_message(mock_message)

    # Assert
    mock_message.answer.assert_called_once()
    # Проверяем, что отправлено сообщение об ошибке
    assert "ошибка" in mock_message.answer.call_args[0][0].lower()
```

## Anti-patterns (что НЕ делать)

### ❌ Множественные сценарии в одном тесте

```python
# ПЛОХО: тестирует 3 разных сценария
def test_config():
    config1 = Config()  # Проверка валидной конфигурации
    assert config1.max_tokens == 1000

    with pytest.raises(ValueError):  # Проверка ошибки
        Config()  # Но без нужных env переменных

    config2 = Config()  # Проверка кастомных значений
    assert config2.temperature == 0.5

# ХОРОШО: 3 отдельных теста
def test_config_valid(valid_env):
    config = Config()
    assert config.max_tokens == 1000

def test_config_missing_token(clean_env):
    with pytest.raises(ValueError, match="TELEGRAM_BOT_TOKEN"):
        Config()

def test_config_custom_temperature(valid_env, monkeypatch):
    monkeypatch.setenv("TEMPERATURE", "0.5")
    config = Config()
    assert config.temperature == 0.5
```

### ❌ Тестирование implementation details

```python
# ПЛОХО: тестирует внутреннюю реализацию
def test_trim_history_internal():
    storage = HistoryStorage(max_history=10)
    storage._trim_history(123)  # Private метод!

# ХОРОШО: тестирует public API
def test_history_limited_to_max(storage):
    """Тест: История обрезается до max_history"""
    user_id = 123
    for i in range(15):
        storage.add_message(user_id, f"Message {i}", "System prompt")

    context = storage.get_context(user_id)
    assert len(context.messages) == 10  # Автоматически обрезано
```

### ❌ Избыточное мокирование

```python
# ПЛОХО: мокируем свой код
def test_conversation_manager_with_mocks():
    mock_storage = Mock()
    mock_formatter = Mock()
    manager = ConversationManager(mock_llm_client, "System prompt")
    manager.storage = mock_storage  # Мокируем свою логику!
    manager.formatter = mock_formatter

# ХОРОШО: тестируем реальную интеграцию
@pytest.mark.asyncio
async def test_process_message(mock_llm_client):
    manager = ConversationManager(mock_llm_client, "System prompt")
    response = await manager.process_message(123, "Hello")

    # Проверяем реальное взаимодействие storage и formatter
    context = manager.storage.get_context(123)
    assert len(context.messages) == 1
```

### ❌ Magic numbers и хардкод

```python
# ПЛОХО: магические числа
def test_something():
    assert result == 42  # Что такое 42?
    assert len(messages) == 3  # Почему 3?

# ХОРОШО: явные константы или вычисляемые значения
def test_history_trim():
    MAX_HISTORY = 10
    storage = HistoryStorage(max_history=MAX_HISTORY)

    messages_count = 15
    for i in range(messages_count):
        storage.add_message(123, f"Message {i}", "System prompt")

    context = storage.get_context(123)
    assert len(context.messages) == MAX_HISTORY
```

### ❌ Зависимость тестов друг от друга

```python
# ПЛОХО: тесты зависят от порядка выполнения
global_storage = None

def test_create_storage():
    global global_storage
    global_storage = HistoryStorage()

def test_add_message():
    global_storage.add_message(123, "Hello", "System prompt")  # Упадет если test_create_storage не выполнился

# ХОРОШО: каждый тест изолирован
@pytest.fixture
def storage():
    return HistoryStorage()

def test_create_storage(storage):
    assert storage is not None

def test_add_message(storage):
    storage.add_message(123, "Hello", "System prompt")
    assert len(storage.get_context(123).messages) == 1
```

## TDD практики

### Начинай с теста

```python
# 1️⃣ Пишем тест ПЕРЕД реализацией
def test_get_user_info_success():
    """Тест: Извлечение user_id и username из Message"""
    bot = TelegramBot(...)
    message = Mock()
    message.from_user = Mock(id=123, username="john_doe", first_name="John")

    user_id, username = bot._get_user_info(message)

    assert user_id == 123
    assert username == "john_doe"

# 2️⃣ Тест падает (метод _get_user_info не существует)

# 3️⃣ Реализуем минимум для прохождения теста
def _get_user_info(self, message: Message) -> tuple[int, str]:
    user_id = message.from_user.id
    username = message.from_user.username or message.from_user.first_name
    return user_id, username

# 4️⃣ Тест проходит - добавляем edge cases
def test_get_user_info_no_username():
    """Тест: Использование first_name если нет username"""
    # ... тест для граничного случая

# 5️⃣ Рефакторим с проверкой на None
def _get_user_info(self, message: Message) -> tuple[int, str]:
    if not message.from_user:
        raise ValueError("Message has no user information")

    user_id = message.from_user.id
    username = message.from_user.username or message.from_user.first_name or "Unknown"
    return user_id, username
```

### Red-Green-Refactor в действии

```python
# 🔴 RED: Тест для нового функционала (падает)
def test_history_storage_trim():
    """История должна обрезаться до max_history"""
    storage = HistoryStorage(max_history=10)

    for i in range(15):
        storage.add_message(123, f"Message {i}", "System prompt")

    context = storage.get_context(123)
    assert len(context.messages) == 10  # Падает - обрезки нет

# 🟢 GREEN: Минимальная реализация (тест проходит)
def add_message(self, user_id: int, text: str, system_prompt: str) -> None:
    context = self.get_or_create_context(user_id, system_prompt)
    context.messages.append(UserMessage(user_id, text, datetime.now()))

    # Простая обрезка
    if len(context.messages) > self.max_history:
        context.messages = context.messages[-self.max_history:]

# 🔵 REFACTOR: Выносим в отдельный метод (DRY)
def add_message(self, user_id: int, text: str, system_prompt: str) -> None:
    context = self.get_or_create_context(user_id, system_prompt)
    context.messages.append(UserMessage(user_id, text, datetime.now()))
    self._trim_history(user_id)

def _trim_history(self, user_id: int) -> None:
    """Обрезать историю до max_history"""
    context = self.contexts.get(user_id)
    if not context:
        return

    if len(context.messages) > self.max_history:
        context.messages = context.messages[-self.max_history:]
    if len(context.responses) > self.max_history:
        context.responses = context.responses[-self.max_history:]
```

### Тестируй граничные случаи

```python
# Не только happy path
def test_get_response_success():
    """✅ Успешный сценарий"""
    pass

# Но и edge cases
def test_get_response_empty_content():
    """✅ Пустой ответ от LLM"""
    pass

def test_get_response_no_usage():
    """✅ Ответ без usage информации"""
    pass

def test_get_response_api_error():
    """✅ Ошибка API"""
    pass

def test_handle_empty_message():
    """✅ Пустое сообщение"""
    pass

def test_handle_long_message():
    """✅ Слишком длинное сообщение"""
    pass
```

## Cheatsheet

### Быстрый старт для нового модуля

```python
# 1. Создай файл теста
touch tests/test_my_module.py

# 2. Импорты
import pytest
from unittest.mock import AsyncMock, Mock, patch
from src.my_module import MyClass

# 3. Фикстуры
@pytest.fixture
def my_instance():
    return MyClass(...)

# 4. Первый тест (TDD: начни с Red)
@pytest.mark.asyncio
async def test_my_method_success(my_instance):
    """Тест: Описание ожидаемого поведения"""
    # Arrange
    expected = "expected result"

    # Act
    result = await my_instance.my_method()

    # Assert
    assert result == expected

# 5. Запусти тест (должен упасть - Red)
pytest tests/test_my_module.py -v

# 6. Реализуй минимум (Green)
# 7. Рефактори (Refactor)
# 8. Добавь edge cases
```

### Команды для ежедневной работы

```bash
# Запустить все тесты
make test

# Запустить конкретный файл
pytest tests/test_config.py -v

# Запустить конкретный тест
pytest tests/test_config.py::test_config_valid -v

# Watch mode (перезапуск при изменении)
pytest-watch

# Coverage для модуля
pytest tests/test_llm_client.py --cov=src.llm_client --cov-report=term-missing

# Показать медленные тесты
pytest --durations=10

# Полная проверка качества
make quality
```

---

**Версия:** 1.0
**Создан:** 2025-10-11
**Принцип:** Test First - Сначала тест, потом код
**Подход:** TDD + KISS + DRY = Quality Tests
