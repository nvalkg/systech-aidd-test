---
alwaysApply: false
---
# Workflow TDD - Test-Driven Development

> **Базовые документы:** [vision.md](../../docs/vision.md) | [conventions.mdc](conventions.mdc) | [qa_conventions.mdc](qa_conventions.mdc) | [roadmap.md](../../docs/roadmap.md)

## 🎯 Принцип TDD

**Test First - Сначала тест, потом код**

```
🔴 RED → 🟢 GREEN → 🔵 REFACTOR → 🔁 REPEAT
```

**Правила TDD:**
1. **НЕ пиши код** без failing теста
2. **НЕ пиши больше теста**, чем достаточно для падения
3. **НЕ пиши больше кода**, чем достаточно для прохождения теста
4. **Рефактори** только после зеленых тестов

---

## 🔄 TDD цикл разработки

### Фаза 🔴 RED - Пишем failing тест

**Цель:** Определить желаемое поведение через тест

**Шаги:**
1. Определи новую функциональность
2. Напиши тест для этой функциональности
3. Запусти `make test` → тест должен упасть (RED)
4. Убедись, что тест падает по правильной причине

**Шаблон RED теста:**
```python
# tests/test_<module>.py

@pytest.mark.asyncio
async def test_<feature_name>_<scenario>() -> None:
    """Тест: <описание желаемого поведения>"""
    # Arrange: подготовка тестовых данных
    instance = MyClass(...)
    test_input = "test data"
    expected_output = "expected result"

    # Act: вызов тестируемого метода
    result = await instance.my_method(test_input)

    # Assert: проверка результата
    assert result == expected_output
```

**Проверка RED:**
```bash
# Запустить тест - должен упасть
pytest tests/test_<module>.py::test_<feature_name> -v

# Вывод должен быть:
# FAILED tests/test_<module>.py::test_<feature_name> - AttributeError: ...
# или
# FAILED tests/test_<module>.py::test_<feature_name> - AssertionError: ...
```

**Критерии готовности RED:**
- ✅ Тест написан и падает
- ✅ Причина падения понятна (нет реализации или неверное поведение)
- ✅ Тест имеет понятное имя и docstring
- ✅ Тест изолирован (используются моки для внешних зависимостей)

---

### Фаза 🟢 GREEN - Минимальная реализация

**Цель:** Сделать тест зеленым минимальным кодом

**Шаги:**
1. Реализуй минимум кода для прохождения теста
2. Не думай об оптимальности - фокус на работоспособности
3. Запусти `make test` → тест должен пройти (GREEN)
4. Убедись, что все старые тесты тоже проходят

**Шаблон GREEN реализации:**
```python
# src/<module>.py

class MyClass:
    async def my_method(self, input_data: str) -> str:
        """Минимальная реализация для прохождения теста"""
        # Простейшая логика, которая проходит тест
        return "expected result"  # Хардкод? Ок для начала!
```

**Проверка GREEN:**
```bash
# Запустить конкретный тест
pytest tests/test_<module>.py::test_<feature_name> -v
# → PASSED

# Запустить все тесты модуля
pytest tests/test_<module>.py -v
# → All PASSED

# Запустить все тесты проекта
make test
# → 42/42 tests passed
```

**Критерии готовности GREEN:**
- ✅ Новый тест проходит
- ✅ Все старые тесты проходят
- ✅ Coverage не упал
- ✅ Код минимален (можно хардкод, можно if/else)

---

### Фаза 🔵 REFACTOR - Улучшение кода

**Цель:** Улучшить код без изменения поведения

**Шаги:**
1. Улучши код (SOLID, DRY, читаемость)
2. Запусти `make test` → все тесты должны оставаться зелеными
3. Запусти `make quality` → проверь lint, typecheck
4. Повтори рефакторинг при необходимости

**Шаблон REFACTOR:**
```python
# src/<module>.py

class MyClass:
    async def my_method(self, input_data: str) -> str:
        """Улучшенная реализация с применением SOLID и DRY"""
        # Заменяем хардкод на реальную логику
        validated_input = self._validate_input(input_data)
        processed_data = self._process_data(validated_input)
        return self._format_output(processed_data)

    def _validate_input(self, data: str) -> str:
        """SRP: Отдельный метод для валидации"""
        if not data or not data.strip():
            raise ValueError("Input cannot be empty")
        return data.strip()

    def _process_data(self, data: str) -> str:
        """SRP: Отдельный метод для обработки"""
        # Реальная бизнес-логика
        return data.upper()

    def _format_output(self, data: str) -> str:
        """SRP: Отдельный метод для форматирования"""
        return f"Processed: {data}"
```

**Проверка REFACTOR:**
```bash
# Все тесты должны проходить
make test
# → 42/42 tests passed, coverage 95%

# Качество кода должно быть отличным
make quality
# → Format: ✓ Lint: ✓ Typecheck: ✓ Test: ✓
```

**Типы рефакторинга:**
- **Extract Method** - вынести логику в отдельный метод
- **Extract Class** - разделить класс (SRP)
- **Remove Duplication** - устранить дублирование (DRY)
- **Rename** - улучшить имена переменных/методов
- **Add Type Hints** - добавить аннотации типов
- **Simplify** - упростить сложную логику

**Критерии готовности REFACTOR:**
- ✅ Все тесты проходят
- ✅ Coverage не упал (лучше вырос)
- ✅ `make lint` → 0 ошибок
- ✅ `make typecheck` → 0 ошибок
- ✅ Код читаемый и поддерживаемый
- ✅ Применены SOLID и DRY принципы

---

## 📋 TDD Workflow для итерации

### Этап 1️⃣: Планирование

**Перед началом:**
- [ ] Изучить задачи из roadmap.md и детального тасклиста спринта
- [ ] Определить список новых функций
- [ ] Для каждой функции определить тестовые сценарии
- [ ] Предложить список тестов (RED фаза)
- [ ] **Дождаться согласования**

**Формат предложения:**
```markdown
## Итерация N: [Название]

### Новые функции
1. **Feature A** - описание
   - Тест: `test_feature_a_success()`
   - Тест: `test_feature_a_invalid_input()`
   - Тест: `test_feature_a_empty_data()`

2. **Feature B** - описание
   - Тест: `test_feature_b_success()`
   - Тест: `test_feature_b_api_error()`

### Порядок реализации (TDD)
1. Feature A → 🔴 RED → 🟢 GREEN → 🔵 REFACTOR
2. Feature B → 🔴 RED → 🟢 GREEN → 🔵 REFACTOR

Согласовать?
```

---

### Этап 2️⃣: Реализация (TDD цикл)

#### 🔴 RED: Написать failing тест

**Для каждой новой функции:**

1. **Создай/открой тестовый файл:**
```bash
vim tests/test_<module>.py
```

2. **Напиши тест:**
```python
@pytest.mark.asyncio
async def test_new_feature_success() -> None:
    """Тест: Новая фича работает корректно"""
    # Arrange
    instance = MyClass(...)
    test_data = "input"

    # Act
    result = await instance.new_feature(test_data)

    # Assert
    assert result == "expected"
```

3. **Запусти тест (должен упасть):**
```bash
pytest tests/test_<module>.py::test_new_feature_success -v
# Expected: FAILED (AttributeError или AssertionError)
```

4. **Проверь причину падения:**
- ✅ Метод не существует → хорошо
- ✅ Метод возвращает неверный результат → хорошо
- ❌ Тест сам с ошибкой → исправь тест

**🔴 RED готово ✓**

---

#### 🟢 GREEN: Реализовать минимум

1. **Создай/открой файл с кодом:**
```bash
vim src/<module>.py
```

2. **Напиши минимальную реализацию:**
```python
async def new_feature(self, input_data: str) -> str:
    """Минимальная реализация"""
    # Самое простое решение
    return "expected"  # Хардкод на первом этапе - ОК!
```

3. **Запусти тест (должен пройти):**
```bash
pytest tests/test_<module>.py::test_new_feature_success -v
# Expected: PASSED
```

4. **Запусти все тесты:**
```bash
make test
# Expected: All tests PASSED
```

**🟢 GREEN готово ✓**

---

#### 🔵 REFACTOR: Улучшить код

1. **Замени хардкод реальной логикой:**
```python
async def new_feature(self, input_data: str) -> str:
    """Улучшенная реализация"""
    if not input_data:
        raise ValueError("Input required")

    processed = self._process(input_data)
    return self._format(processed)

def _process(self, data: str) -> str:
    """SRP: обработка данных"""
    return data.upper()

def _format(self, data: str) -> str:
    """SRP: форматирование"""
    return f"Result: {data}"
```

2. **Добавь type hints:**
```python
async def new_feature(self, input_data: str) -> str:
    """Типизированная реализация"""
    ...
```

3. **Запусти проверки:**
```bash
# Тесты должны проходить
make test
# → PASSED

# Линтер должен быть доволен
make lint
# → All checks passed!

# Type checker должен быть доволен
make typecheck
# → Success: no issues found
```

4. **Проверь coverage:**
```bash
pytest tests/test_<module>.py --cov=src.<module> --cov-report=term-missing
# → 100% coverage для нового метода
```

**🔵 REFACTOR готово ✓**

---

#### 🔁 REPEAT: Следующая функция

**Повтори цикл для следующей функции:**
1. 🔴 RED → новый тест
2. 🟢 GREEN → минимальная реализация
3. 🔵 REFACTOR → улучшение кода

**Когда все функции реализованы:**
```bash
# Финальная проверка
make quality

# Ожидаемый результат:
# ✓ Format check passed
# ✓ Lint check passed
# ✓ Type check passed
# ✓ Tests: 42/42 passed
# ✓ Coverage: 95%
```

---

### Этап 3️⃣: Проверка

**После завершения TDD циклов:**

1. **Запусти полную проверку:**
```bash
make quality
```

2. **Проверь coverage:**
```bash
make test
# Убедись: coverage >= 80% (лучше >90%)
```

3. **Проверь документацию:**
- [ ] Docstrings добавлены для новых методов
- [ ] Type hints для всех публичных методов
- [ ] Комментарии для сложной логики

4. **Покажи результаты:**
```markdown
## Результаты итерации N

### Реализованные функции
- ✅ Feature A: 3 теста, 100% coverage
- ✅ Feature B: 2 теста, 100% coverage

### Метрики качества
- Tests: 47/47 passed (+5 новых)
- Coverage: 96% (было 95%)
- Lint: All checks passed
- Typecheck: Success

### Команды для проверки
\`\`\`bash
make quality
pytest tests/test_<module>.py -v
\`\`\`
```

5. **Дождаться подтверждения**

---

### Этап 4️⃣: Фиксация

**После подтверждения:**

1. **Обновить roadmap.md и тасклист спринта:**
```markdown
| Итерация | Статус | Описание | Дата завершения |
|----------|--------|----------|-----------------|
| N | ✅ Done | Новые функции A, B | 2025-10-11 |
```

2. **Создать коммит:**
```bash
git add .
git commit -m "feat: iteration N - feature A and B

- Добавлена feature A с валидацией
- Добавлена feature B с обработкой ошибок
- Покрытие тестами: 96%
- TDD: 5 новых тестов, все проходят"
```

3. **Согласовать переход к следующей итерации**

---

## 🎯 TDD практические примеры

### Пример 1: Новый метод с валидацией

#### 🔴 RED Phase
```python
# tests/test_config.py

def test_config_parse_bool_valid_true(valid_env, monkeypatch):
    """Тест: парсинг булевого значения 'true'"""
    monkeypatch.setenv("ENABLE_FEATURE", "true")

    config = Config()
    result = config.parse_bool("ENABLE_FEATURE", default=False)

    assert result is True

def test_config_parse_bool_invalid(valid_env, monkeypatch):
    """Тест: ошибка при невалидном булевом значении"""
    monkeypatch.setenv("ENABLE_FEATURE", "not_a_bool")

    with pytest.raises(ValueError, match="должно быть булевым значением"):
        config = Config()
        config.parse_bool("ENABLE_FEATURE", default=False)
```

**Запуск:**
```bash
pytest tests/test_config.py::test_config_parse_bool_valid_true -v
# → FAILED: AttributeError: 'Config' object has no attribute 'parse_bool'
```

✅ **RED фаза завершена** - тест падает по правильной причине

---

#### 🟢 GREEN Phase
```python
# src/config.py

class Config:
    def parse_bool(self, key: str, default: bool = False) -> bool:
        """Парсинг булевого значения из env"""
        value = os.getenv(key)
        if value is None:
            return default

        # Минимальная реализация
        if value.lower() == "true":
            return True
        elif value.lower() == "false":
            return False
        else:
            raise ValueError(f"{key} должно быть булевым значением (true/false)")
```

**Запуск:**
```bash
pytest tests/test_config.py::test_config_parse_bool_valid_true -v
# → PASSED

pytest tests/test_config.py::test_config_parse_bool_invalid -v
# → PASSED

make test
# → All tests PASSED
```

✅ **GREEN фаза завершена** - тесты проходят

---

#### 🔵 REFACTOR Phase
```python
# src/config.py

class Config:
    # Константы для булевых значений
    TRUE_VALUES = {"true", "1", "yes", "on"}
    FALSE_VALUES = {"false", "0", "no", "off"}

    def parse_bool(self, key: str, default: bool = False) -> bool:
        """
        Парсинг булевого значения из переменной окружения.

        Args:
            key: Имя переменной окружения
            default: Значение по умолчанию

        Returns:
            bool: Распарсенное значение

        Raises:
            ValueError: Если значение не является булевым
        """
        value = os.getenv(key)
        if value is None:
            return default

        value_lower = value.lower().strip()

        if value_lower in self.TRUE_VALUES:
            return True
        elif value_lower in self.FALSE_VALUES:
            return False
        else:
            valid_values = ", ".join(self.TRUE_VALUES | self.FALSE_VALUES)
            raise ValueError(
                f"{key} должно быть булевым значением ({valid_values}), "
                f"получено: {value}"
            )
```

**Проверка:**
```bash
make test
# → All tests PASSED

make lint
# → All checks passed!

make typecheck
# → Success: no issues found
```

✅ **REFACTOR фаза завершена** - код улучшен, тесты проходят

---

### Пример 2: Async метод с мокированием API

#### 🔴 RED Phase
```python
# tests/test_llm_client.py

@pytest.mark.asyncio
async def test_get_response_handles_none_content(llm_client: LLMClient) -> None:
    """Тест: обработка None в content ответа"""
    # Arrange: мок с None content
    mock_response = Mock(
        choices=[Mock(message=Mock(content=None))],
        usage=Mock(total_tokens=10)
    )

    # Act & Assert
    with patch.object(
        llm_client.client.chat.completions, "create", new_callable=AsyncMock
    ) as mock_create:
        mock_create.return_value = mock_response

        result = await llm_client.get_response([{"role": "user", "content": "Hi"}])

        # None должен стать пустой строкой
        assert result == ""
```

**Запуск:**
```bash
pytest tests/test_llm_client.py::test_get_response_handles_none_content -v
# → FAILED: AssertionError: assert None == ""
```

✅ **RED фаза завершена**

---

#### 🟢 GREEN Phase
```python
# src/llm_client.py

async def get_response(self, messages: list[dict[str, Any]]) -> str:
    """Получить ответ от LLM"""
    response = await self.client.chat.completions.create(
        model=self.model,
        messages=messages,
        max_tokens=self.max_tokens,
        temperature=self.temperature,
    )

    content = response.choices[0].message.content

    # Минимальная реализация для прохождения теста
    if content is None:
        content = ""

    return content
```

**Запуск:**
```bash
pytest tests/test_llm_client.py::test_get_response_handles_none_content -v
# → PASSED

make test
# → All tests PASSED
```

✅ **GREEN фаза завершена**

---

#### 🔵 REFACTOR Phase
```python
# src/llm_client.py

async def get_response(self, messages: list[dict[str, Any]]) -> str:
    """
    Получить ответ от LLM через OpenRouter API.

    Args:
        messages: История сообщений для контекста

    Returns:
        str: Ответ от LLM (пустая строка если None)

    Raises:
        Exception: При ошибке API
    """
    try:
        response = await self.client.chat.completions.create(
            model=self.model,
            messages=messages,  # type: ignore[arg-type]
            max_tokens=self.max_tokens,
            temperature=self.temperature,
        )

        # Извлечение content с обработкой None
        content = response.choices[0].message.content
        result = content if content is not None else ""

        # Логирование
        usage = response.usage
        if usage:
            logger.info(f"LLM response: {usage.total_tokens} tokens used")

        return result

    except Exception as e:
        logger.error(f"Ошибка при запросе к LLM: {e}")
        raise
```

**Проверка:**
```bash
make quality
# → All checks passed!
```

✅ **REFACTOR фаза завершена**

---

## 🚀 TDD Cheatsheet

### Быстрый старт новой функции

```bash
# 1. RED: Напиши тест
vim tests/test_<module>.py
# Добавь: def test_new_feature_success(): ...

# 2. Запусти (должен упасть)
pytest tests/test_<module>.py::test_new_feature_success -v
# → FAILED ✓

# 3. GREEN: Реализуй минимум
vim src/<module>.py
# Добавь: def new_feature(): return "expected"

# 4. Запусти (должен пройти)
pytest tests/test_<module>.py::test_new_feature_success -v
# → PASSED ✓

# 5. REFACTOR: Улучши код
vim src/<module>.py
# Примени SOLID, DRY, добавь type hints

# 6. Проверь качество
make quality
# → All checks passed ✓
```

### Команды для TDD workflow

```bash
# Запустить конкретный тест
pytest tests/test_<module>.py::test_<name> -v

# Запустить все тесты модуля
pytest tests/test_<module>.py -v

# Запустить с coverage
pytest tests/test_<module>.py --cov=src.<module> --cov-report=term-missing

# Watch mode (автоперезапуск при изменении)
pytest-watch tests/test_<module>.py

# Полная проверка качества
make quality
```

### Проверка фаз TDD

**🔴 RED checklist:**
- [ ] Тест написан с понятным именем
- [ ] Docstring описывает сценарий
- [ ] Тест падает (`pytest ... -v` → FAILED)
- [ ] Причина падения правильная (нет реализации/неверный результат)

**🟢 GREEN checklist:**
- [ ] Минимальная реализация добавлена
- [ ] Новый тест проходит (`pytest ... -v` → PASSED)
- [ ] Все старые тесты проходят (`make test` → all PASSED)
- [ ] Coverage не упал

**🔵 REFACTOR checklist:**
- [ ] Код улучшен (SOLID, DRY)
- [ ] Type hints добавлены
- [ ] Все тесты проходят (`make test` → all PASSED)
- [ ] Lint без ошибок (`make lint` → passed)
- [ ] Typecheck без ошибок (`make typecheck` → success)
- [ ] Coverage вырос или остался на уровне

---

## 🎓 TDD Best Practices

### Золотые правила

1. **Всегда начинай с RED**
   - Не пиши код без failing теста
   - Тест падает = ты знаешь, что реализовать

2. **GREEN - минимум кода**
   - Хардкод? ОК для начала
   - If/else? Отлично
   - Главное - тест проходит

3. **REFACTOR без страха**
   - Тесты зеленые = можно рефакторить
   - Сломал тесты? Откати изменения
   - Тесты - твоя страховка

4. **Маленькие шаги**
   - Один тест = одна функция
   - Один коммит = один TDD цикл
   - Легче отлаживать, легче ревьюить

5. **Тестируй поведение, не реализацию**
   - Тестируй public API
   - Не тестируй private методы напрямую
   - Фокус на "что", а не "как"

### Когда применять TDD

✅ **Обязательно:**
- Новая функциональность
- Исправление багов
- Рефакторинг существующего кода
- Критичная бизнес-логика

⚠️ **Опционально:**
- Прототипирование (spike solutions)
- Исследовательский код
- Временные скрипты

❌ **Не нужно:**
- Тривиальный код (getters/setters)
- Внешние библиотеки (они уже протестированы)
- UI layout (лучше визуальное тестирование)

### Типичные ошибки

❌ **Пишешь код перед тестом**
```python
# ПЛОХО: сначала код, потом тест
def new_feature():  # Написал код
    return "result"

def test_new_feature():  # Потом тест
    assert new_feature() == "result"
```

✅ **Правильно: тест перед кодом**
```python
# ХОРОШО: сначала тест
def test_new_feature():  # 🔴 RED
    assert new_feature() == "result"

# Потом код
def new_feature():  # 🟢 GREEN
    return "result"
```

---

❌ **Слишком большой шаг**
```python
# ПЛОХО: сразу сложная реализация
def test_calculate_complex():
    result = calculator.calculate(
        operations=["add", "multiply", "divide"],
        values=[1, 2, 3, 4, 5]
    )
    assert result == 42
```

✅ **Правильно: маленькие шаги**
```python
# ХОРОШО: начни с простого
def test_calculate_add():
    result = calculator.add(1, 2)
    assert result == 3

def test_calculate_multiply():
    result = calculator.multiply(2, 3)
    assert result == 6

# Потом комбинируй
def test_calculate_chain():
    result = calculator.chain([("add", 1, 2), ("multiply", 3)])
    assert result == 9
```

---

❌ **Рефакторинг на RED**
```python
# ПЛОХО: рефакторинг когда тесты красные
def test_new_feature():
    assert new_feature() == "result"
# → FAILED

# Начинаешь рефакторить старый код... ❌
```

✅ **Правильно: рефакторинг только на GREEN**
```python
# ХОРОШО: сначала GREEN
def test_new_feature():
    assert new_feature() == "result"

def new_feature():
    return "result"  # Минимум
# → PASSED ✓

# Теперь можно рефакторить ✓
```

---

## 📊 Метрики TDD

### Отслеживай прогресс

**После каждого TDD цикла:**
```bash
# Количество тестов
pytest --collect-only | grep "test session starts"
# Before: 42 tests
# After: 45 tests (+3)

# Coverage
make test
# Before: 94%
# After: 96% (+2%)

# Скорость выполнения
pytest --durations=10
# Все тесты < 1s = отлично
```

**Целевые метрики:**
- Tests: +3-5 тестов на функцию
- Coverage: >95% для нового кода
- Speed: <1s на unit тест
- Quality: 0 lint errors, 0 type errors

---

## 🔗 Интеграция с CI/CD

### GitHub Actions для TDD

**При каждом push:**
```yaml
# .github/workflows/quality.yml
- name: Run tests
  run: |
    make test
    # Проверяет все TDD тесты
```

**Требования для merge:**
- ✅ All tests passed
- ✅ Coverage >= 95%
- ✅ No lint errors
- ✅ No type errors

### Локальный pre-commit

**Перед каждым коммитом:**
```bash
# В корне проекта
git add .
make quality  # Обязательно!
git commit -m "feat: new feature via TDD"
```

**Если `make quality` упал:**
```bash
# Не коммить! Сначала исправь
# 1. Проверь что упало
make test      # Тесты?
make lint      # Линтер?
make typecheck # Типы?

# 2. Исправь проблемы
# 3. Повтори
make quality

# 4. Теперь коммить
git commit -m "feat: new feature via TDD"
```

---

## ✅ Checklist для TDD итерации

### Перед началом
- [ ] Изучены задачи из roadmap.md и тасклиста спринта
- [ ] Определены новые функции
- [ ] Список тестов подготовлен
- [ ] План согласован

### Для каждой функции

**🔴 RED:**
- [ ] Тест написан
- [ ] Тест падает
- [ ] Причина падения правильная

**🟢 GREEN:**
- [ ] Минимальная реализация
- [ ] Новый тест проходит
- [ ] Все тесты проходят

**🔵 REFACTOR:**
- [ ] Код улучшен (SOLID, DRY)
- [ ] Type hints добавлены
- [ ] Тесты проходят
- [ ] Lint без ошибок
- [ ] Typecheck без ошибок

### После всех функций
- [ ] `make quality` → все проверки passed
- [ ] Coverage >= 95%
- [ ] Документация обновлена
- [ ] Результаты показаны разработчику
- [ ] Подтверждение получено
- [ ] Коммит создан
- [ ] roadmap.md и тасклист спринта обновлены

---

**Версия:** 1.0
**Создан:** 2025-10-11
**Принцип:** Test First - RED → GREEN → REFACTOR → REPEAT
**Подход:** TDD - качество через тесты
