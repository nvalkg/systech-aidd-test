# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_from_botfather

# OpenRouter API Configuration
OPENROUTER_API_KEY=your_openrouter_api_key

# LLM Settings (optional, defaults provided)
DEFAULT_MODEL=openai/gpt-3.5-turbo
MAX_TOKENS=1000
TEMPERATURE=0.7

# Bot Settings (optional, defaults provided)
MAX_HISTORY_MESSAGES=10

# System Prompt (optional, choose one method)
# Method 1: Direct text
SYSTEM_PROMPT=You are a helpful AI assistant.

# Method 2: Load from file (uncomment to use)
# SYSTEM_PROMPT_FILE=prompts/system_prompt.txt

# Database Configuration
# For local development with Docker Compose (default)
DATABASE_URL=postgresql+asyncpg://aidd:aidd_dev_password@localhost:5432/aidd

# For production or custom setup, adjust as needed:
# DATABASE_URL=postgresql+asyncpg://user:password@host:port/database

# Logging (optional, default: INFO)
LOG_LEVEL=INFO
