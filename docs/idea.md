# LLM-ассистент в виде Telegram-бота

## Описание проекта

Специализированный AI-продукт в виде Telegram-бота с **конкретной ролью** и четко обозначенной функцией. Бот выполняет узкоспециализированные задачи в своей области, определяемой системным промптом.

### Концепция продукта

**От универсального ассистента к специализированному AI-продукту:**
- Не "помощник для всего", а **эксперт в конкретной области**
- Четко определенная роль и зона ответственности
- Оптимизированный системный промпт для специфичной функции
- Пользователь получает качественную помощь в **одной конкретной задаче**

## Основные характеристики

### Функциональность

#### Базовые возможности
- **Специализированный диалог**: Общение строго в рамках заданной роли
- **Экспертные ответы**: Глубокие знания в конкретной области
- **Фиксированная роль**: Поведение определяется системным промптом из файла
- **Контекстная память**: Сохранение истории диалога (до 10 сообщений)

#### Команды бота
- `/start` - приветствие и описание роли бота
- `/help` - справка по использованию и возможностям
- `/role` - отображение текущей роли и описания функций
- `/clear` - очистка истории диалога

### Технические требования

#### Текущая реализация (MVP)
- **Платформа**: Telegram Bot API (aiogram 3.x)
- **Язык**: Python 3.11+
- **LLM**: OpenRouter API (доступ к различным моделям)
- **Хранение**: В памяти (без БД для MVP)
- **Архитектура**: Простая, монолитная, KISS принцип

#### Системный промпт
- **Хранение**: В отдельном файле `prompts/system_prompt.txt`
- **Загрузка**: При старте бота или через переменную окружения
- **Гибкость**: Легкая замена промпта без изменения кода
- **Формат**: Plain text с четким описанием роли и инструкций

## Архитектура системы

### Компоненты (MVP - KISS подход)

1. **TelegramBot** (`telegram_bot.py`) - обработка команд и сообщений
2. **ConversationManager** (`conversation_manager.py`) - управление диалогом
3. **LLMClient** (`llm_client.py`) - взаимодействие с OpenRouter API
4. **Config** (`config.py`) - конфигурация из `.env` файла
5. **HistoryStorage** (`history_storage.py`) - хранение истории в памяти
6. **MessageFormatter** (`message_formatter.py`) - форматирование для API
7. **PromptLoader** (планируется) - загрузка системного промпта из файла

### Поток данных

```
Пользователь
    ↓
Telegram API
    ↓
TelegramBot (команды: /start, /help, /role, /clear)
    ↓
ConversationManager (управление контекстом)
    ↓
HistoryStorage + MessageFormatter
    ↓
LLMClient (запрос к OpenRouter)
    ↓
LLM Response
    ↓
Telegram → Пользователь
```

### Команда /role - Детали реализации

**Назначение:**
- Показать пользователю текущую роль бота
- Объяснить специализацию и возможности
- Помочь пользователю понять, в чем бот является экспертом

**Реализация:**
```python
async def cmd_role(self, message: Message) -> None:
    """Показать роль и функции бота"""
    role_info = self._get_role_description()
    await message.answer(role_info)
```

**Формат ответа:**
```
🎭 Моя роль: [Название роли]

📋 Описание:
[Детальное описание специализации]

✨ Я могу помочь вам:
• [Функция 1]
• [Функция 2]
• [Функция 3]

💡 Пример использования:
[Пример запроса пользователя]

⚙️ Модель: [название модели]
```

## Специализированные роли (примеры)

### Концепция специализации
Вместо универсального ассистента создается **AI-продукт с конкретной ролью**:

#### Технические специалисты
- **Python Code Reviewer** - ревью кода, best practices, рефакторинг
- **DevOps Consultant** - помощь с CI/CD, Docker, Kubernetes
- **Security Auditor** - анализ безопасности кода и инфраструктуры
- **API Designer** - проектирование REST/GraphQL API

#### Креативные специалисты
- **Technical Writer** - написание документации, README, tutorials
- **Content Strategist** - стратегия контента для продуктов
- **UX Copywriter** - тексты для интерфейсов
- **Email Marketing Assistant** - письма, кампании, A/B тесты

#### Бизнес-специалисты
- **Product Manager Assistant** - PRD, user stories, roadmaps
- **Data Analyst** - анализ данных, insights, визуализация
- **Customer Success Manager** - поддержка клиентов, onboarding
- **Sales Copy Expert** - продающие тексты, landing pages

#### Образовательные специалисты
- **Programming Tutor** - обучение программированию (конкретный язык)
- **Math Explainer** - объяснение математики, решение задач
- **Language Teacher** - изучение иностранных языков
- **Career Coach** - карьерное развитие, CV, интервью

### Пример системного промпта (файл `prompts/system_prompt.txt`)

```
Роль: Python Code Reviewer Expert

Ты - опытный Python разработчик с 10+ годами опыта, специализирующийся на code review и рефакторинге. Твоя задача - помогать разработчикам улучшать качество кода.

Твои принципы:
- SOLID, DRY, KISS - основа твоих рекомендаций
- PEP 8 и современные Python практики
- Type hints и документация обязательны
- Тестируемость и читаемость превыше всего

Твои функции:
1. Ревью кода с конкретными рекомендациями
2. Рефакторинг с объяснением изменений
3. Поиск багов и потенциальных проблем
4. Советы по архитектуре и паттернам
5. Best practices для Python 3.11+

Формат ответа:
- Начинай с общей оценки (Good/Needs Improvement/Critical Issues)
- Группируй замечания по категориям (Critical/Important/Nice to have)
- Давай конкретные примеры кода
- Объясняй "почему", а не только "как"

Ограничения:
- Фокус только на Python коде
- Не пишешь код с нуля (только ревью и рефакторинг)
- Не решаешь общие вопросы по программированию
```

## Технический стек (текущая реализация)

### Основной стек (MVP)
- **Python 3.11+** - современная версия с улучшенной производительностью
- **aiogram 3.x** - асинхронная библиотека для Telegram Bot API
- **openai** - официальный клиент для работы с OpenRouter API
- **python-dotenv** - загрузка переменных окружения
- **uv** - быстрый менеджер зависимостей (замена pip)

### Инструменты качества кода
- **ruff** - форматтер + линтер (замена black + flake8)
- **mypy** - статический анализ типов (strict mode)
- **pytest** - тестирование с pytest-asyncio
- **pytest-cov** - coverage метрики (цель >80%)

### Архитектурные принципы
- **KISS** - максимальная простота, без оверинжиниринга
- **SOLID** - особенно Single Responsibility Principle
- **DRY** - устранение дублирования кода
- **Type Safety** - 100% type hints для публичных методов
- **TDD** - разработка через тесты (Test-Driven Development)

## Структура проекта (текущая)

```
systech-aidd-test/
├── .cursor/
│   └── rules/
│       ├── conventions.mdc      # Правила разработки
│       ├── workflow.mdc         # Workflow основной
│       ├── workflow_tdd.mdc     # Workflow TDD
│       └── qa_conventions.mdc   # Правила тестирования
├── .github/
│   └── workflows/
│       └── quality.yml          # CI/CD pipeline
├── docs/
│   ├── idea.md                  # Концепция проекта
│   ├── vision.md                # Техническое видение
│   ├── roadmap.md               # Дорожная карта проекта
│   └── tasklists/               # Детальные планы спринтов
├── prompts/
│   └── system_prompt.txt        # Системный промпт роли (планируется)
├── src/
│   ├── __init__.py
│   ├── __main__.py              # Entry point для python -m src
│   ├── config.py                # Конфигурация из .env
│   ├── models.py                # Dataclass модели
│   ├── llm_client.py            # OpenRouter API клиент
│   ├── history_storage.py       # Хранение истории в памяти
│   ├── message_formatter.py     # Форматирование для LLM
│   ├── conversation_manager.py  # Управление диалогом
│   ├── telegram_bot.py          # Telegram бот (команды + обработка)
│   └── main.py                  # Точка входа
├── tests/
│   ├── conftest.py              # Фикстуры pytest
│   ├── test_config.py           # Тесты конфигурации
│   ├── test_models.py           # Тесты моделей
│   ├── test_llm_client.py       # Тесты LLM клиента
│   ├── test_history_storage.py  # Тесты хранилища
│   ├── test_message_formatter.py # Тесты форматтера
│   ├── test_conversation_manager.py # Тесты менеджера
│   ├── test_telegram_bot.py     # Тесты бота
│   └── test_main.py             # Интеграционные тесты
├── .env                         # Локальные настройки (не в git)
├── .env.example                 # Шаблон переменных
├── .gitignore
├── pyproject.toml               # Конфигурация проекта + зависимости
├── uv.lock                      # Lockfile зависимостей
├── Makefile                     # Команды для разработки
├── README.md                    # Документация проекта
└── CONTRIBUTING.md              # Руководство для разработчиков
```

## Конфигурация

### Переменные окружения (.env)

**Обязательные:**
```bash
# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token  # От @BotFather

# OpenRouter
OPENROUTER_API_KEY=your_openrouter_api_key  # От openrouter.ai
```

**Опциональные (есть defaults):**
```bash
# LLM настройки
DEFAULT_MODEL=openai/gpt-3.5-turbo  # Модель по умолчанию
MAX_TOKENS=1000                      # Максимум токенов в ответе
TEMPERATURE=0.7                      # Креативность модели (0-1)

# Бот настройки
MAX_HISTORY_MESSAGES=10              # Макс сообщений в истории
SYSTEM_PROMPT=You are a helpful AI assistant.  # Системный промпт
# Или путь к файлу:
SYSTEM_PROMPT_FILE=prompts/system_prompt.txt  # (планируется)

# Логирование
LOG_LEVEL=INFO                       # DEBUG, INFO, WARNING, ERROR
```

### Команды Make

```bash
# Установка зависимостей
make install      # uv sync (первый запуск)

# Разработка
make run          # Запуск бота
make dev          # Запуск через uv run

# Качество кода
make format       # Автоформатирование (ruff format)
make lint         # Проверка кода (ruff check)
make typecheck    # Проверка типов (mypy)
make test         # Запуск тестов + coverage
make quality      # Все проверки сразу (format + lint + typecheck + test)

# Очистка
make clean        # Удалить кэш и временные файлы
```

## Планы развития

### ✅ MVP - Реализовано (v1.0)
1. ✅ Базовый Telegram бот на aiogram 3.x
2. ✅ Интеграция с OpenRouter API
3. ✅ Диалог с сохранением контекста (10 сообщений)
4. ✅ Команды: `/start`, `/help`, `/clear`
5. ✅ Качество кода: 95% test coverage, CI/CD
6. ✅ Архитектура: SOLID, DRY, type safety

### 🔄 Следующая итерация (v1.1)
1. **Команда `/role`** - отображение роли и возможностей
   - Показ системного промпта (краткое описание)
   - Список функций бота
   - Примеры использования
   - Информация о модели

2. **Загрузка промпта из файла**
   - Директория `prompts/`
   - Файл `system_prompt.txt` с ролью
   - Переменная окружения `SYSTEM_PROMPT_FILE`
   - Fallback на `SYSTEM_PROMPT` из .env

3. **PromptLoader класс**
   - Загрузка промпта при старте
   - Валидация формата промпта
   - Метод `get_role_description()` для команды `/role`

### 🚀 Расширенные функции (v2.0+)

#### Специализация роли
1. **Библиотека промптов** - готовые роли для разных сценариев
2. **Валидация запросов** - проверка соответствия запроса специализации
3. **Контекстные подсказки** - помощь в формулировке запросов
4. **Шаблоны ответов** - форматирование под тип роли

#### Персонализация
1. **Настройки пользователя** - сохранение предпочтений
2. **История сессий** - сохранение между перезапусками (БД)
3. **Мультиязычность** - поддержка разных языков
4. **Кастомизация роли** - пользователь может настроить поведение

#### Аналитика и улучшения
1. **Метрики использования** - популярные запросы, время ответа
2. **Feedback система** - пользователи оценивают ответы
3. **A/B тестирование** - тестирование разных промптов
4. **Улучшение промпта** - на основе feedback

#### Продвинутые возможности
1. **Файлы и документы** - обработка загруженных файлов (для Code Reviewer)
2. **Streaming responses** - постепенная отправка длинных ответов
3. **Команды с параметрами** - `/role detailed`, `/clear last 5`
4. **Inline mode** - использование бота в любом чате
5. **Web-интерфейс** - управление ботом через веб

## Безопасность

### Текущая реализация
- ✅ Валидация токенов при старте (fail fast)
- ✅ Обработка ошибок с понятными сообщениями
- ✅ Логирование без токенов и personal data
- ✅ Ограничение длины сообщений (защита от спама)

### Планируется
- ⏳ Rate limiting на уровне пользователя
- ⏳ Фильтрация нежелательного контента
- ⏳ Whitelist/blacklist пользователей
- ⏳ Ограничение по токенам (лимит расходов)
- ⏳ Audit log для критичных операций

## Мониторинг и логирование

### Текущая реализация
- ✅ Консольное логирование (INFO, ERROR)
- ✅ Структурированные сообщения
- ✅ Логирование запросов к LLM (модель, токены)
- ✅ Обработка ошибок с деталями

### Планируется
- ⏳ Sentry для отслеживания ошибок
- ⏳ Prometheus метрики
- ⏳ Grafana дашборды
- ⏳ Алерты на критичные ошибки
- ⏳ Retention policy для логов

## Примеры использования

### Сценарий 1: Python Code Reviewer Bot

**Пользователь отправляет:**
```python
def calculate(a, b):
    result = a + b
    return result
```

**Бот отвечает:**
```
🔍 Code Review: Needs Improvement

📋 Critical Issues:
✗ Нет type hints для параметров и возвращаемого значения
✗ Нет docstring

💡 Important:
• Имя функции слишком общее (calculate что?)
• Параметры a, b не описывают смысл

✅ Рефакторинг:
def add_numbers(first: int, second: int) -> int:
    """Сложить два числа и вернуть результат."""
    return first + second

📚 Why:
- Type hints улучшают читаемость и позволяют IDE находить ошибки
- Docstring документирует назначение функции
- Понятные имена делают код self-documenting
```

### Сценарий 2: Technical Writer Bot

**Пользователь отправляет:**
```
Нужна документация для API endpoint:
POST /users
Создает нового пользователя
```

**Бот отвечает:**
```
📝 API Documentation Draft

## Create User

Creates a new user in the system.

**Endpoint:** `POST /api/v1/users`

**Authentication:** Required (Bearer token)

### Request Body

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| email | string | Yes | User's email address (unique) |
| name | string | Yes | Full name (3-100 characters) |
| role | string | No | User role (default: "user") |

**Example Request:**
```json
{
  "email": "user@example.com",
  "name": "John Doe",
  "role": "admin"
}
```

### Response

**Success (201 Created):**
```json
{
  "id": "uuid",
  "email": "user@example.com",
  "name": "John Doe",
  "role": "admin",
  "created_at": "2024-01-15T10:30:00Z"
}
```

**Error (400 Bad Request):**
```json
{
  "error": "validation_error",
  "message": "Email already exists"
}
```

### Notes
- Emails must be unique
- Password will be sent via email
- Rate limit: 10 requests per minute

💡 Want me to add error codes section?
```

---

## Заключение

Проект эволюционирует от универсального LLM-ассистента к **специализированному AI-продукту** с четкой ролью и функциями.

### Ключевые преимущества специализации:
- ✅ **Экспертность** - глубокие знания в конкретной области
- ✅ **Качество** - оптимизированный промпт для специфичных задач
- ✅ **Простота** - пользователь знает, для чего предназначен бот
- ✅ **Эффективность** - точные ответы без лишней информации

### Следующие шаги:
1. Реализовать команду `/role` (v1.1)
2. Добавить загрузку промпта из файла
3. Создать библиотеку готовых ролей
4. Собрать feedback от пользователей

---

**Документ создан:** 2024-01-15
**Последнее обновление:** 2025-10-11
**Версия:** 2.0 (Specialized AI Product)
**Статус:** Активная разработка (v1.0 MVP готов, v1.1 в планах)
